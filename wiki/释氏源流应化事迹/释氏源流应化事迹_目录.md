---
title: "释氏源流应化事迹(目录)"
---
{% include collection-nav.html %}

# {{ page.title }}
出了什么事情？  
```
{% for doc in site.documents %}
    URL: {{ doc.url }}          # 文档URL
    PATH: {{ doc.path }}         # 文件路径
    TITLE: {{ doc.title }}        # 标题
{% endfor %}
```
文章数量：{{ site.documents | size }}
我也不知道耶～  
再试一次吧。
```
{% for page in site.pages %}
    URL: {{ doc.url }}          # 文档URL
    PATH: {{ doc.path }}         # 文件路径
    TITLE: {{ doc.title }}        # 标题
{% endfor %}
```
文章数量：{{ site.pages | size }}
好吧。

{% comment %} 自动收集本文集的所有文章 {% endcomment %}
{% assign collection_docs = site.documents %}
文章数量：{{ collection_docs | size }}
<table>
<tr>
{% for doc in collection_docs %}
    {% assign title_rest = doc.title | split: "(" | last %}
    {% assign episode_id = title_rest | split: ")" | first %}
    {% assign episode_name = title_rest | split: ")" | last %}
    
    <td style="padding: 10px; vertical-align: top; width: 25%;">
        <a href="/{{ doc.url }}">
            {% if episode_id == "目录" %}
                文集目录
            {% else %}
                {{ episode_id }}  {{ episode_name }}
            {% endif %}
        </a>
    </td>
    
    {% comment %} 每4个换行 {% endcomment %}
    {% assign index = forloop.index0 | plus: 1 %}
    {% if index % 4 == 0 %}
        </tr><tr>
    {% endif %}
{% endfor %}
</tr>
</table>