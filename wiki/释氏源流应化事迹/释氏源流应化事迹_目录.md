---
title: "释氏源流应化事迹(目录)"
---
{% include collection-nav.html %}

# {{ page.title }}

{% comment %} 自动收集本文集的所有文章 {% endcomment %}
{% assign collection_docs = site.documents | where_exp: "doc", "doc.path contains '释氏源流应化事迹/释氏源流应化事迹_'" | sort: "path" %}

<table>
<tr>
{% for doc in collection_docs %}
    {% assign title_rest = doc.title | split: "(" | last %}
    {% assign episode_id = title_rest | split: ")" | first %}
    {% assign episode_name = title_rest | split: ")" | last %}
    
    <td style="padding: 10px; vertical-align: top; width: 25%;">
        <a href="/{{ doc.url }}">
            {% if episode_id == "目录" %}
                文集目录 {{ doc.name }}
            {% else %}
                {{ episode_id }}  {{ episode_name }}  {{ doc.name}}
            {% endif %}
        </a>
    </td>
    
    {% comment %} 每4个换行 {% endcomment %}
    {% assign index = forloop.index0 | plus: 1 %}
    {% if index % 4 == 0 %}
        </tr><tr>
    {% endif %}
{% endfor %}
</tr>
</table>